<!-- Page Heading/Breadcrumbs -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Softhouses <small>list</small></h1>
        <ol class="breadcrumb">
            <li><a href="/">Home</a>
            </li>
            <li class="active">Softhouses</li>
        </ol>
    </div>
</div>
<!-- /.row -->

<!-- Content Row -->
<div class="row text-center row-same-height">

    <% @softhouses.each do |s| %>
        <div class="col-md-3 col-sm-6 hero-feature">
            <div class="thumbnail thumbnail-games">
                <%=image_tag(s.image.url(:medium), width: 350, height: 180) if s.image.present? %>
                <div class="caption">
                    <h3><%=s.name%></h3>
                    <p><a href="/softhouse/<%=s.id%>" class="btn btn-default">Read More</a></p>
                </div>
            </div>
        </div>
    <% end %>

</div>
<!-- /.row -->

<div class="row">
    <%= paginate @softhouses %>
</div>